<!DOCTYPE html>
<html>
<head>
    <title>Background Color Changer</title>

    <style>
        body {
            background: #f2f4f8;
            font-family: Arial, Helvetica, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            transition: background-color 0.4s ease;
        }

        .card {
            background: white;
            padding: 40px;
            width: 350px;
            border-radius: 14px;
            box-shadow: 0 4px 18px rgba(0,0,0,0.15);
            text-align: center;
        }

        h2 {
            color: #333;
            margin-bottom: 20px;
            font-size: 24px;
        }

        img {
            border-radius: 12px;
            width: 220px;
            height: 150px;
            object-fit: cover;
            box-shadow: 0 3px 10px rgba(0,0,0,0.25);
        }

        button {
            padding: 12px 28px;
            font-size: 18px;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            background: #4a90e2;
            color: white;
            margin-top: 25px;
            transition: 0.3s;
        }

        button:hover {
            background: #357ac8;
            transform: scale(1.07);
        }

        .tagline {
            font-size: 14px;
            margin-top: 15px;
            color: #555;
        }
    </style>

    <script>
        var colors = ["#b3e5fc", "lightgreen", "lightpink", "orange", "violet", "yellow"];
        var index = 0;

        function changeColor() {
            document.body.style.backgroundColor = colors[index];
            index++;
            if (index == colors.length) {
                index = 0;
            }
        }
    </script>
</head>

<body>

    <div class="card">
        <h2>🎨 Enjoy Beautiful Background Colors</h2>

        <img src="tulip1.jpg" alt="Flower Image">

        <button onclick="changeColor()">Change Color</button>

        <p class="tagline">Click the button and watch the page transform!</p>
    </div>

</body>
</html>
